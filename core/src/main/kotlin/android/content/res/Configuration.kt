package android.content.res

/**
 * Provide a minimal configuration stub for preview annotations on the JVM.
 */
class Configuration {
    companion object {
        /** Night mode UI flag used by preview annotations. */
        const val UI_MODE_NIGHT_YES: Int = 0x20
    }
}
